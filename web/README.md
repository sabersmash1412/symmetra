# Symmetra Web

Browser-hosted MVP for facial movement monitoring.

## Local Development

```bash
npm install
npm run dev
```

Open `http://localhost:3000`.

The app runs MediaPipe Face Landmarker in the browser and stores saved session summaries locally in browser storage.
